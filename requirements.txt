fastapi==0.115.6
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
pymysql==1.1.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
celery==5.4.0
redis==5.0.8
minio==7.2.9
chromadb==0.5.17
pypdf==5.0.1
python-docx==1.1.2
Pillow==10.4.0
pytesseract==0.3.13
python-multipart==0.0.12
pytest==8.3.3
httpx==0.27.2
openai
pymupdf>=1.23.0
google-auth
google-auth-oauthlib
google-api-python-client
alembic
rank-bm25==0.2.2
gliner2==1.3.2
# transformers pinned explicitly (not left to the resolver): gliner2==1.3.2's
# mDeBERTa-v3 tokenizer init crashes on transformers<5.0.0 (AttributeError:
# 'list' object has no attribute 'keys', inside _set_model_specific_special_tokens)
# — 4.57.6 is the newest 4.x release, so no 4.x version fixes this. Bumping
# past that also requires sentence-transformers>=5.2.0, the first release
# that widens its own pin from transformers<5.0.0 to <6.0.0 (verified via
# PyPI metadata across 4.0.0-5.6.1). Both pins verified together: full
# `pip install -r requirements.txt` resolves with no conflicts, and GLiNER2
# + the CrossEncoder reranker both load and run correctly on this combo.
transformers==5.0.0
sentence-transformers==5.2.0
neo4j==5.24.0
deep-translator==1.11.4


